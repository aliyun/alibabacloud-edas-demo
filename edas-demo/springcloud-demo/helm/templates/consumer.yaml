kind: Deployment
apiVersion: apps/v1
metadata:
  name: consumer
  namespace: {{ .Values.apps.namespace }}
  labels:
    cloudapp.alibabacloud.com/app: consumer
  annotations:
    cloudapp.alibabacloud.com/namespace: {{ .Values.apps.microNamespace }}
    cloudapp.alibabacloud.com/immediate-rollout: "true"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: consumer
  template:
    metadata:
      labels:
        app: consumer
    spec:
      containers:
        - image: beihang-registry-vpc.cn-beijing.cr.aliyuncs.com/{{ .Values.apps.imageNamespace }}/consumer:2.0
          name: consumer
          ports:
            - containerPort: 18082
              name: http
          resources:
            limits:
              cpu: "1"
              memory: 1Gi
            requests:
              cpu: "1"
              memory: 1Gi
