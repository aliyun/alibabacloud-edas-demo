kind: Deployment
apiVersion: apps/v1
metadata:
  name: provider
  namespace: {{ .Values.apps.namespace }}
  labels:
    cloudapp.alibabacloud.com/app: provider
  annotations:
    cloudapp.alibabacloud.com/namespace: {{ .Values.apps.microNamespace }}
    cloudapp.alibabacloud.com/immediate-rollout: "true"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: provider
  template:
    metadata:
      labels:
        app: provider
    spec:
      containers:
        - image: beihang-registry-vpc.cn-beijing.cr.aliyuncs.com/{{ .Values.apps.imageNamespace }}/provider:2.0
          name: provider
          ports:
            - containerPort: 18081
              name: http
          resources:
            limits:
              cpu: "1"
              memory: 1Gi
            requests:
              cpu: "1"
              memory: 1Gi
